{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions required by the sheet corrector window.",
  "windows": ["main"],
  "permissions": ["core:event:default", "core:webview:default", "core:window:default"]
}
