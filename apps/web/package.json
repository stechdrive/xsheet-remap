{
  "name": "@xsheet-remap/web",
  "version": "0.1.652",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:version": "node ../../tools/version/sync-version.mjs",
    "predev": "npm run sync:version",
    "prebuild": "npm run sync:version",
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:pages": "tsc -b && vite build --mode pages && node ../../tools/pages/prepare-pages.mjs",
    "preview": "vite preview",
    "preview:pages": "vite preview --outDir dist-pages"
  },
  "dependencies": {
    "@xsheet-remap/adapters": "*",
    "@xsheet-remap/core": "*",
    "@xsheet-remap/ui": "*",
    "@xsheet-remap/xdts": "*"
  },
  "devDependencies": {},
  "license": "MIT"
}
